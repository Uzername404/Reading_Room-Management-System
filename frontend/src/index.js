import React from "react";
import ReactDOM from "react-dom";
import App from "./assets/App"; // Adjust the path if necessary
import "./index.css"; // Optional, for global styles

ReactDOM.render(<App />, document.getElementById("root"));